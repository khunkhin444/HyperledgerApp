```
connection-profile.js: connection profile for the network

config.json: configuration file for the application

enrollAdmin.js: enroll the admin user

registerUser.js: register the user

package.json: node.js dependencies

server.js: node.js server

wallet: directory to store the identity of the user

utils: directory to store the utility functions: utility functions for fabric

wallet-Orderer: directory to store the identity of the orderer

wallet-Org1: directory to store the identity of the org1

wallet-Org2: directory to store the identity of the org2

```