```
cryptogen: to generate crypto-config.yaml which is used to generate crypto material for the network
configtxgen: to generate configtx.yaml which is used to generate genesis block and channel configuration transaction
docker-compose: to start the network
cli: to interact with the network
```